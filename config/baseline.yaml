# Baseline CNN encoder configuration

# Model architecture
encoder_type: cnn
feat_dim: 256
hid: 256
emb: 128

# Training hyperparameters
epochs: 8
batch_size: 128
lr: 0.0002
max_len: 8

# Data
data_root: ./data

# Output
out: runs
